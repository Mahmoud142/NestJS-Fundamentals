version: '3.8'

services:
  minify-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: minify_api_app
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
